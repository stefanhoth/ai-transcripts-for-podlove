<?xml version="1.0"?>
<ruleset name="AI Transcripts for Podlove">
    <description>WordPress coding standards for this plugin.</description>

    <file>.</file>
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>
    <exclude-pattern>tests/*</exclude-pattern>
    <exclude-pattern>assets/*</exclude-pattern>

    <rule ref="WordPress">
        <!-- Project uses PSR-4 autoloading via Composer; WP file naming would break it -->
        <exclude name="WordPress.Files.FileName"/>
        <!-- PHP 8 type hints are the source of truth; @param/@return are redundant -->
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamComment"/>
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingReturn"/>
    </rule>

    <config name="minimum_supported_wp_version" value="6.4"/>
    <config name="text_domain" value="ai-transcripts-for-podlove"/>
</ruleset>
